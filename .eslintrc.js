module.exports = {
  root: true,
  extends: [
    'prettier',
    'eslint:recommended',
    'next/core-web-vitals',
    'plugin:tailwindcss/recommended',
  ],
  ignorePatterns: ['ui'], // ignore all files generated by shadcn/ui cli
  plugins: ['prettier', 'import', 'prefer-arrow', 'react', 'sort-destructure-keys', 'tailwindcss'],
  rules: {
    'no-unused-vars': [
      'error',
      {
        varsIgnorePattern: '^_',
        argsIgnorePattern: '^_',
      },
    ],
    'prettier/prettier': 'warn',
    'arrow-body-style': ['warn', 'as-needed'],
    'padding-line-between-statements': [
      'warn',
      { blankLine: 'always', prev: '*', next: ['return', 'throw'] },
      { blankLine: 'always', prev: '*', next: ['block-like', 'class'] },
      { blankLine: 'always', prev: ['block-like', 'class'], next: '*' },
      { blankLine: 'any', prev: '*', next: ['case', 'default'] },
      { blankLine: 'always', prev: ['var', 'let', 'const'], next: '*' },
      {
        blankLine: 'any',
        prev: ['var', 'let', 'const'],
        next: ['var', 'let', 'const'],
      },
    ],
    'prefer-arrow/prefer-arrow-functions': [
      'warn',
      {
        disallowPrototype: true,
        singleReturnOnly: false,
        classPropertiesAllowed: false,
      },
    ],
  },
  overrides: [
    {
      files: ['*.ts', '*.tsx'],
      parser: '@typescript-eslint/parser',
      parserOptions: {
        project: ['./tsconfig.json'],
        tsconfigRootDir: __dirname,
      },
    },
  ],
};
